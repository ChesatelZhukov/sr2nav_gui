#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
РОЗОВАЯ БИМБО / БАРБИ — самая гламурная тема для интерфейса!

Цвета как у любимой куклы:
- Основной фон — сочный розовый (как коробка от Барби)
- Акценты — горячий розовый, фиолетовый, золотой
- Текст — белый и тёмно-розовый для контраста
- Всё остальное — просто ОЧЕНЬ РОЗОВО!
"""
from dataclasses import dataclass


@dataclass(frozen=True)
class Theme:
    """
    САМАЯ РОЗОВАЯ ТЕМА В МИРЕ 💖
    
    Все цвета представлены в формате HEX-строк.
    """
    
    # ============ ОСНОВНЫЕ ФОНЫ ============
    # Тут всё розовое, и точка!
    BG_PRIMARY: str = "#FFB6C1"       # Светло-розовый (Light Pink) — основной фон
    BG_SECONDARY: str = "#FFC0CB"      # Нежно-розовый (Pink) — панели
    BG_TERTIARY: str = "#FFA6C9"       # Тёплый розовый — выделение
    
    # ============ ЦВЕТА ТЕКСТА ============
    FG_PRIMARY: str = "#000000"        # Белый — читаем на розовом
    FG_SECONDARY: str = "#000000"      # Горячий розовый (Hot Pink)
    FG_DISABLED: str = "#000000"       # Бледно-розовый — отключено
    
    # ============ ГРАНИЦЫ И РАЗДЕЛИТЕЛИ ============
    BORDER: str = "#FF1493"            # Глубокий розовый (Deep Pink)
    
    # ============ АКЦЕНТНЫЕ ЦВЕТА ============
    # Да, ACCENT_BLUE теперь тоже розовый! ПОТОМУ ЧТО МЫ ТАК ХОТИМ!
    ACCENT_BLUE: str = "#FF69B4"       # Это не синий, это розовый! Hot Pink
    ACCENT_GREEN: str = "#FF85B3"      # Розовый (раньше был зелёным)
    ACCENT_RED: str = "#FF4D6D"        # Ярко-розовый (раньше был красным)
    ACCENT_ORANGE: str = "#FFA07A"     # Светло-лососевый (был оранжевый)
    ACCENT_PURPLE: str = "#DA70D6"     # Орхидея (розово-фиолетовый)
    ACCENT_CYAN: str = "#FFB3C6"       # Бледно-розовый (был бирюзовый)
    
    # ============ СТАТУСНЫЕ ЦВЕТА ============
    SUCCESS: str = "#FFC0CB"           # Розовый — успех (почему бы и нет?)
    WARNING: str = "#FFB347"           # Персиковый — предупреждение
    ERROR: str = "#FF6B8B"             # Светло-розово-красный — ошибка
    INFO: str = "#FFB6C1"              # Светло-розовый — информация
    DEBUG: str = "#FFA6C9"             # Тёплый розовый — отладка
    
    # ============ СОСТОЯНИЯ ЭЛЕМЕНТОВ ============
    HOVER: str = "#FF1493"             # Глубокий розовый — наведение
    SELECTED: str = "#FF69B4"           # Горячий розовый — выбрано
    DISABLED: str = "#FFB6C1"           # Бледно-розовый — отключено


def apply_theme(widget) -> None:
    """
    Рекурсивно применяет тему к виджету и всем его дочерним элементам.
    ВСЁ БУДЕТ РОЗОВЫМ! 🌸
    """
    bg = widget.cget('bg')
    if bg in ('SystemButtonFace', 'SystemWindow', '#f0f0f0'):
        try:
            widget.configure(bg=Theme.BG_PRIMARY)
        except:
            pass

    try:
        for child in widget.winfo_children():
            apply_theme(child)
    except:
        pass